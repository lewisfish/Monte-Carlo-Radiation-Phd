      subroutine iarray(xface,yface,zface,rhokap)

      include 'grid.txt'


c**** Initialize array values to be zero

        xface=0.
        yface=0.
        zface=0.
        rhokap=0.
                    
      return
      end
